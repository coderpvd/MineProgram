<!--pages/home/home.wxml-->
<!-- 1.小程序的数据绑定 {{}} -->
<view>Hello:{{name}}</view>
<view>年龄:{{age}}</view>

<!-- 2.小程序的列表展示 -->
<view wx:for="{{students}}" id="item">{{item.id}}--{{item.name}}--{{item.age}}</view>

<!-- 3.事件监听改变data -->
<view> counter:{{counter}}</view>
<button size="mini" bind:tap='hedleBtnClick'>+</button>
<button size="mini" bind:tap="hedleSubClick">-</button>